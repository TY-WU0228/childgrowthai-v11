[build]
  publish = "."
  command = "echo 'No build required for ChildGrowth AI static app'"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"


[[headers]]
  for = "/index.html"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"
